# Copiar a secrets.toml (local) o pegar en Streamlit Cloud → Settings → Secrets.

# Contador de auditorías (opcional)
# [usage]
# get_url = "https://script.google.com/macros/s/.../exec"
# increment_url = "https://script.google.com/macros/s/.../exec"

# Valoraciones de usuarios → Google Sheets
[feedback]
submit_url = "https://script.google.com/macros/s/REEMPLAZAR/exec"
# token = "token-opcional-mismo-que-en-SAVTFeedback.gs"
